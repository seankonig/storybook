<header>
    <div class="collapse bg-dark" id="navbarHeader">
        <div class="container">
            <div class="row">
                {{#if user}}
                <div class="col-sm-8 col-md-7 py-4">
                    <h5 class="text-white">Menu</h5>
                    <ul class="list-unstyled">
                        <li><a href="/dashboard" class="text-white"><i class="fas fa-tachometer-alt"></i> Dashboard</a></li>
                        <li><a href="/stories" class="text-white"><i class="fas fa-book-reader"></i> View All Posts</a></li>
                        <li><a href="/stories/add" class="text-white"><i class="fas fa-plus"></i> Create Post</a></li>
                        <li><a href="/user/edit/{{user._id}}" class="text-white"><i class="fas fa-user-circle"></i> Edit Profile</a></li>
                        <li><a href="/auth/logout" class="text-white"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
                    </ul>
                </div>
                {{else}}
                <div class="col-sm-4 offset-md-1 py-4">
                    <h5 class="text-white">Please Login/Sign up</h4>
                    <ul class="list-unstyled">
                        <a href="/auth/google" class="btn btn-danger my-2"><i class="fab fa-google"></i> Login</a>
                    </ul>
                </div>
                {{/if}}
            </div>
        </div>
    </div>
    <div class="navbar navbar-dark bg-dark box-shadow">
        <div class="container d-flex justify-content-between">
            <a href="/dashboard" class="navbar-brand d-flex align-items-center">
                <strong>🚀 blogger</strong>
            </a>
            {{#if user}}
            <code class="navbar-brand abs">Hi {{user.firstName}}</code>
            {{/if}}
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarHeader"
                aria-controls="navbarHeader" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
    </div>
</header>